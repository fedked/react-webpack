import React from 'react';
import ReactDOM from 'react-dom'

const rootElement = document.getElementById('root');
const App = () => {
    return <h1>Olá Mundo dev</h1>
}

ReactDOM.render(<App />, rootElement)